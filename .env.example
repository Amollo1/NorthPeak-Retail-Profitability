# Copy this file to ".env" in the same folder and fill in your real values.
# .env should NEVER be committed to GitHub — it is already listed in .gitignore.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=northpeak_retail
DB_USER=northpeak_analyst
DB_PASSWORD=your_password_here
